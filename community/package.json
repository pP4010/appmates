{
  "name": "launchpilot-community",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply launchpilot-community --local",
    "migrate:remote": "wrangler d1 migrations apply launchpilot-community --remote"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
